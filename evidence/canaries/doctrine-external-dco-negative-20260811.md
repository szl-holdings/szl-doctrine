# Intentional negative DCO canary

This branch exists only to prove that the protected external Doctrine DCO controller fails closed when a candidate commit has no `Signed-off-by` trailer.

It must never be merged. The PR will be closed and the branch deleted after the exact workflow and Evaluate ruleset receipts are recorded.
